import type { ThemeFontSizes } from '../../models/theme.js';

/**
 * Shared font-size ladder used by every preset theme.
 */
export const standardFontSizes: ThemeFontSizes = {
  title: 36,
  heading: 28,
  body: 18,
  code: 14,
  small: 12,
};
