import type { ThemePreset } from '../../models/theme-preset.js';

/** blue：ocean + academic + legacy */
export const bluePreset: ThemePreset = {
  name: 'blue',
  colorScheme: 'ocean',
  textScheme: 'academic',
  layoutScheme: 'legacy',
};
