import type { ThemeStyles } from '../../../models/theme.js';

/** legacy 装饰开关：与改造前硬编码一致 */
export const legacyLayoutStyles: ThemeStyles = {
  sectionBarWidth: 0.15,
  footerDivider: false,
  coverBadge: false,
};
