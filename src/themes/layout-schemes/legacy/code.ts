import type { ThemeLayoutSpec } from '../../../models/theme.js';

/** 旧版代码页：边距 0.5、标题区约 0.7 */
export const codeLayout: ThemeLayoutSpec = {
  titleAlign: 'left',
  margin: 0.5,
  titleHeight: 0.7,
};
