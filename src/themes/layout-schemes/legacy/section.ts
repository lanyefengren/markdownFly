import type { ThemeLayoutSpec } from '../../../models/theme.js';

/** 旧版章节页：左侧装饰条 0.15 + 标题左对齐 */
export const sectionLayout: ThemeLayoutSpec = {
  accentBar: 'left',
  titleAlign: 'left',
  margin: 1.0,
};
