import type { ThemeStyles } from '../../../models/theme.js';

/** folio 装饰：章节走双线，不用左侧竖条宽度；页脚分割线开 */
export const folioLayoutStyles: ThemeStyles = {
  sectionBarWidth: 0.04,
  footerDivider: true,
  coverBadge: false,
};
